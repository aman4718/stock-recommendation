# ── CPU-only PyTorch (install before sentence-transformers) ───────
# Dockerfile/render.yaml handle this as a separate step:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# ── Embeddings ───────────────────────────────────────────────────
sentence-transformers==3.0.1

# ── LLM — Groq free tier ─────────────────────────────────────────
groq==0.28.0

# ── Web scraping ─────────────────────────────────────────────────
requests==2.32.3
beautifulsoup4==4.12.3

# ── Backend API ──────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.0
pydantic==2.10.6

# ── Utilities ────────────────────────────────────────────────────
python-dotenv==1.0.1
loguru==0.7.2
numpy==1.26.4
